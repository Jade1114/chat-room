1. 当前项目是在线实时频道聊天室，我们的设计应该是：在不进入频道时也能接受对应频道的消息（通过一个小点或类似的方式来提醒用户）。目前在离开频道之后直接看不到对应频道的消息了。我们后续需要调整成能接受没有显示的频道的消息。
2. 目前的系统消息会一直显示 xxx进入频道，退出频道。这个感觉没必要在前端显示？
3. 在发送消息时，如果我使用中文输出法，通过回车将拼音转为普通字母时，会出发回车的发送消息，导致额外的消息发送。
4. 前端布局有问题：在小尺寸屏幕上，右侧的在线成员会直接被切割掉不显示，这应该是布局采取了固定布局的原因，需要调整成响应式或者其他类型的布局。
5.
